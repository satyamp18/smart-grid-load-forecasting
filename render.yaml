services:
  - type: web
    name: smart-grid-api
    runtime: python

    buildCommand: |
      pip install --no-cache-dir -r requirements.txt

    startCommand: |
      uvicorn smartgrid.main:app --host 0.0.0.0 --port ${PORT:-10000}

    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
      - key: DATABASE_URL
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: SKIP_DB_INIT
        value: "false"